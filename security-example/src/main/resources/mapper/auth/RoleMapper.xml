<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.security.auth.mapper.RoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.security.auth.entity.Role">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="description" property="description" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, description
    </sql>

    <select id="findByResourceId" resultMap="BaseResultMap">
        select
        r.id,
        r.name,
        r.description
        from role r
        inner join resource_role rr on rr.role_id = r.id
        where rr.resource_id = #{resourceId}
    </select>

    <select id="findRolesByUserId" resultMap="BaseResultMap">
        select
        r.id,
        r.name,
        r.description
        from role r
        inner join user_role ur on ur.role_id = r.id
        where ur.user_id = #{userId}
    </select>
</mapper>
